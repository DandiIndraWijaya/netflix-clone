import { createTheme } from '@mui/material';

const theme = createTheme({
  palette: {
    primary: {
      main: '#B81D24',
      light: '#E50914',
      dark: '#221F1F',
      white: '#f5f5f1',
    },
  },
});

export default theme;
